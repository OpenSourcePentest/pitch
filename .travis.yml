language: ruby
before_install:
 - gem install asciidoctor-reveal.js
script:
 - asciidoctor-reveal.js 
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    branch: gh-pages

